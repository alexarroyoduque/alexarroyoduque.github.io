{
  "name": "po-main",
  "version": "1.0.0",
  "description": "po-main",
  "repository": "https://github.com/alexarroyoduque/alexarroyoduque.github.io",
  "main": "po-main.js",
  "scripts": {
    "serve": "npx web-dev-server --config web-dev-server.config.js",
    "build": "node esbuild.config.js",
    "optimize-images": "npx gulp",
    "prepare-deploy": "npm run build && npm run optimize-images",
    "clean": "rm -rf index.html && rm -rf build",
    "deploy": "npm run clean && npm run prepare-deploy"
  },
  "author": "AlexArroyoDuque",
  "license": "licencia de Creative Commons Reconocimiento-NoComercial-CompartirIgual 4.0 Internacional",
  "dependencies": {
    "@webcomponents/webcomponentsjs": "^2.0.0",
    "lit": "^3.0.0"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.6",
    "esbuild": "^0.23.0",
    "fs-extra": "^11.2.0",
    "gulp": "^4.0.0",
    "gulp-imagemin": "^9.0.0"
  }
}
