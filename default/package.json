{
  "name": "po-main",
  "version": "1.0.0",
  "description": "po-main",
  "repository": "https://github.com/alexarroyoduque/alexarroyoduque.github.io",
  "main": "po-main.js",
  "scripts": {
    "build": "polymer build",
    "prepare-deploy": "npm run build && npm run copy-default && npm run copy-index && npm run update-references",
    "copy-default": "cp -r build/default ./",
    "copy-index": "cp default/src/index.html ./",
    "update-references": "node scripts/update-references.js",
    "deploy": "rm -rf index.html && rm -rf default && npm run prepare-deploy"
  },
  "author": "AlexArroyoDuque",
  "license": "licencia de Creative Commons Reconocimiento-NoComercial-CompartirIgual 4.0 Internacional",
  "dependencies": {
    "@webcomponents/webcomponentsjs": "^2.0.0",
    "lit": "^2.0.0"
  },
  "devDependencies": {
    "polymer-cli": "1.9.11"
  }
}
